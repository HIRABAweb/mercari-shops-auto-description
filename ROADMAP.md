# ロードマップ

このプロジェクトは Human-in-the-loop を前提にします。AI が生成した出品データは、そのまま公開用データとして扱わず、人間が確認・修正したうえでメルカリShops用CSVにします。

## レビュー運用の方向性

現在の `review_required.csv` は、メルカリShopsへアップロードするCSVではなく、確認が必要な項目を人間に知らせるためのチェックリストです。

今後は、AIが作った下書きデータを人間が確認・修正し、承認済みの商品だけから最終的なメルカリShops用CSVを生成する運用へ進めます。

| フェーズ | 目的 | 運用者の作業 | システム出力 | 補足 |
| --- | --- | --- | --- | --- |
| Phase 1 | Google Sheetsをレビュー画面として使う | Sheets上で生成結果と確認理由を見て、商品名・説明文・カテゴリ・ブランド等を修正し、承認済みにする | 下書き出品シート、レビュー理由シート、承認済みメルカリShops用CSV | 最短で運用を楽にする段階。最終CSVにはメルカリShops仕様外の列を混ぜない |
| Phase 2 | 簡易Webレビュー画面を作る | バッチを開き、画像と確認理由を見ながらフォームで修正し、商品ごとに承認する | `draft_listing.json`、`review_required.json`、`approved_listing.json`、バッチCSV | CSVやSheetsを直接編集する負担を減らす |
| Phase 3 | 本格的な運用管理画面にする | バッチ管理、担当者管理、レビュー状況、変更履歴、再生成、CSV出力を画面で扱う | 承認済みCSV、監査ログ、運用ダッシュボード | 認証、UI、保存設計、デプロイが必要 |

## Phase 1 の目標

Phase 1では、既存のメルカリShops用CSV仕様を壊さず、レビューと承認の層を追加します。

| 出力 | 目的 | メルカリShopsへアップロードするか |
| --- | --- | --- |
| 下書き出品データ | AIが生成した承認前の商品データ | しない |
| レビュー理由データ | 採寸なし、カテゴリ不明、タイトル短縮など、人間が確認すべき理由 | しない |
| 承認済みメルカリShops用CSV | 人間が確認・修正した後の最終アップロード用CSV | する |

レビュー状態は次のように扱います。

| ステータス | 意味 |
| --- | --- |
| `needs_review` | 確認が必要な項目がある |
| `approved` | 運用者が出力してよいと確認した |
| `hold` | まだ出力しない |
| `rejected` | この生成結果は使わない |

## Phase 1 の作業項目

| 優先度 | 状態 | 作業項目 | 理由 |
| --- | --- | --- | --- |
| P0 | 実装済み | Sheets append前に一意キーを確認して冪等にする | Cloud Functionsのリトライで同じ商品が重複登録されるのを防ぐ |
| P0 | 実装済み | MercariプロンプトのGCS読み込み失敗時は停止する | 汎用フォールバックプロンプトで仕様外の出力を作らない |
| P0 | 一部実装済み | `exports/{batch_id}/{item_id}/` のようなバッチprefixを導入する | 今日分と翌日分のreview集約が混ざるのを防ぐ |
| P0 | 実装済み | 商品単位reviewファイルをバッチprefix配下に出す | reviewデータをアップロード単位で閉じる |
| P1 | 実装済み | 専用のレビューシートを作る | 最終CSVの列仕様を壊さずに確認理由と承認状態を扱う |
| P1 | 実装済み | 承認済み行だけからメルカリShops用CSVを生成する | `batch_prefix` 指定に対応し、未確認商品や別batchの商品を誤ってアップロードしない |
| P1 | 実装済み | review集約・CSV出力用の明示的なentrypointを追加する | 本番運用でいつ何が出力されるかを明確にする |

## Phase 1 前のマージ判断

現状の修正をmainへマージする前に、次を確認または修正します。

| 項目 | 状態 | 理由 |
| --- | --- | --- |
| Sheets appendの冪等性 | 実装済み | append前に既存の一意キーを確認して、リトライ時の重複appendを抑止する |
| runtimeプロンプト失敗時の停止 | 実装済み | prompt設定ミスや権限エラーを隠さないため |
| バッチ境界の導入 | 一部実装済み | review出力と集約はbatch prefixに対応済み。実運用ではアップロード先を `exports/{batch_id}/{item_id}/` に揃える必要がある |
| 集約トリガーの明確化 | 実装済み | `aggregate_review_required_on_marker` を別entrypointとしてデプロイすると、`_SUCCESS.txt` または `_processed.txt` のイベントで対象batch prefixだけ集約判定できる |
| README / 運用手順 | 更新済み | `review_required.csv` は確認リストであり、最終アップロードは承認済みCSVで行うことを明確にするため |
