# Codex entry point

- The authoritative AI instructions are in the root `AGENTS.md`; read it before starting.
- Run the shared verification with `python scripts/check.py`.
- Unapproved external operations and deployments are prohibited. Cloud Run deployment is allowed only after the exact explicit approval process in `AGENTS.md`.
- Codex must never change IAM, IAP, Secrets, APIs, Projects, or buckets.
